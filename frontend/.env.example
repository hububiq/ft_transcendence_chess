# LOCAL DEVELOPMENT (SOLO TESTING)
# Use these values when building and testing on a single machine.
# Uncomment this section and comment out the LAN MULTIPLAYER section below.

VITE_BASE_API_URL="http://localhost:8000"       # Django
VITE_FASTAPI_URL="http://localhost:8001"        # FastAPI REST
VITE_WS_BASE_URL="ws://localhost:8001"          # FastAPI WebSockets

# ---

# LAN MULTIPLAYER (42 CAMPUS / 1v1 TESTING)
# Use these values when testing on two different computers.
# Replace <server-ip> with the Host computer's actual network IP address.
#
# HOW TO FIND YOUR IP:
# - Linux/macOS (42 Campus): Open terminal and run `hostname -I` or `ipconfig getifaddr en0`
# - Windows (Host): Open Command Prompt and run `ipconfig` (Look for IPv4)
#
# IMPORTANT: Both players must use the SAME Host IP in this file.

# VITE_BASE_API_URL="http://<server-ip>:8000"
# VITE_FASTAPI_URL="http://<server-ip>:8001"
# VITE_WS_BASE_URL="ws://<server-ip>:8001"

# ---

# AUTHENTICATION & OAUTH
# Your GitHub OAuth Client ID for login.
# Note: If switching to LAN multiplayer, make sure to update your GitHub 
# OAuth App settings to accept your <server-ip> as the callback URL!

VITE_GITHUB_CLIENT_ID="your_github_client_id_here"